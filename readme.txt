ѕрограмма выравнивает отступы в исходном коде.
ѕока работает только при условии, что фигурные скобки в начале строки
TODO: сделать игнорирование комментариев, литералов и провер€ть скобки не только в начале строки, но и в конце
 ак и с удалением комментариев использовать через потоки, например:
./makepretty < in.cpp > out.cpp
Ћибо можно перенаправл€ть поток с одной программы на другую, т.е.:
»з deletecomments переводим в makepretty и выводим куда-либо: 
./deletecomments < main.cpp | ./makepretty > nice.cpp